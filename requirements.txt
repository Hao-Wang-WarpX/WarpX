# MCP framework (FastMCP + ToolError)
mcp[cli]>=1.2,<2.0

# HTTP client (async + HTTP/2)
httpx[http2]>=0.27,<1.0

# Search (was duckduckgo-search, renamed to ddgs in 2025-03)
ddgs>=9.0,<10.0

# HTML main content extraction (built-in boilerplate removal)
trafilatura>=1.7,<3.0

# HTML -> Markdown for renderer output
markdownify>=0.13,<1.0
beautifulsoup4>=4.12,<5.0
lxml>=5.2,<6.0

# JS rendering
playwright>=1.47,<2.0

# Image validation
Pillow>=10.4,<12.0

# Config
pydantic>=2.8,<3.0
pydantic-settings>=2.4,<3.0
python-dotenv>=1.0,<2.0

# Retry decorator
tenacity>=8.5,<10.0

# Dev / Test
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<2.0
